---
- name: download packages
  dnf: 
    name: "{{ item }}"
  with_items:
    - firewalld
    - python-firewall

- name: enable firewall
  service:
    name: firewalld
    state: started
    enabled: yes

- name: Open new ssh port
  firewalld:
    port: "{{ ssh_port }}/tcp"
    state: enabled
    permanent: true
